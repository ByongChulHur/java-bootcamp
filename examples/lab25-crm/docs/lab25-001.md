# Lab 25 AI review log (`lab25-001`)

- Suggestion accepted: N/A — no AI tool used; all layering done manually.
- Suggestion rejected (and why): N/A — but for reference, would reject Service returning
  ResponseEntity (Service must stay HTTP-free) or Controller importing Repository directly
  (bypasses Service).
- Or mark N/A if manual only: N/A — manual implementation only.

## JPA readiness note

CustomerRepository would become `extends JpaRepository<Customer, String>`, with
InMemoryCustomerRepository replaced by Spring Data's generated implementation.
CustomerService and CustomerController stay unchanged since Service depends only
on the CustomerRepository interface, not the concrete implementation.

## Failure Experiments (Lab 25)

| # | Experiment | Result | Notes |
| - | ---------- | ------ | ----- |
| 1 | Repeat GET CUS-1001 | 200, same data every time | Idempotent, as expected |
| 2 | Duplicate POST CUS-1001 | 500 Internal Server Error | Service threw IllegalStateException("Duplicate customer"); no exception mapping yet, so it surfaces as 500 — expected for this lab's scope |
| 3 | GET CUS-9999 (not-found) | 500 Internal Server Error | Service threw IllegalArgumentException("Customer not found: CUS-9999"); different exception type than duplicate, proving the two failure modes are distinguishable at the service layer |