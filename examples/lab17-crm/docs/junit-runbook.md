# Lab 17 — JUnit / JaCoCo runbook

## Commands

```bash
mvn -B test
mvn -B clean verify
```

## Coverage gate

- Package: `com.northstar.crm.service`
- Minimum line ratio: **0.80**
- Deliberately fail once with `0.99`, then restore `0.80`

## Coverage goal

- Target: 80% line coverage on com.northstar.crm.service
- Actual: 97%

## Test
- CustomerServiceTests — 6 tests (Duplicate Id, email, listall, and etc)
- CustomerValidatorParameterizedTest — 12 tests (6 legal and 6 illegal status transition)
- GlobalExceptionHandlerTest — 2 tests (404 and 409)

## Evidence

- Surefire reports: `target/surefire-reports/` (20 tests, 0 failures, 0 errors)
- JaCoCo HTML report: `target/site/jacoco/index.html`
- JaCoCo raw data: `target/jacoco.exec`
- Last green `mvn clean verify`: BUILD SUCCESS, `com.northstar.crm.service` line ratio ≥ 0.80
- Deliberate gate-fail proof: minimum temporarily set to 0.99 →
  "lines covered ratio is 0.97, but expected minimum is 0.99" →
  BUILD FAILURE → restored to 0.80 → BUILD SUCCESS

## Copilot review policy

All of the products that are generated by AI should be logged with a dated review before commit any work. 

## Last coverage gap closed by

`duplicateEmailThrowsConflict` and `listAllReturnsBothCustomers` which were added in step 5, 
these covered the email-uniqueness check and the listAll() method, which had no tests before.
