# Lab 26 — Profile YAML TODOs

## Required files
need 4. Basis, dev, test, and prod

## Base keys
spring.application.name: northstar-crm
server.port: 8080

## dev example key
logging.level.com.northstar: DEBUG

## prod secret pattern
spring.datasource.password: ${DB_PASSWORD}